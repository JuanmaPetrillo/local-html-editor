# Local HTML Presentation Editor

A fully local editor for interactive HTML presentations generated by Microsoft 365 Copilot Chat.

The product goal is simple: a non-technical user should be able to open a downloaded HTML presentation, edit visible text, replace images, move/resize elements with drag-and-drop, preview the result, and export a working HTML file again without sending sensitive content to any third party.

## Product principles

1. **Full local execution**: presentation files, drafts, assets, logs, and exports stay on the user's machine.
2. **No admin requirement for end users**: the distributed app must run/install under a normal Windows user profile.
3. **No-code editing**: the primary experience must feel like a presentation editor, not an IDE.
4. **Safe by default**: imported HTML is untrusted. It must never gain access to privileged app APIs or broad filesystem access.
5. **Preserve first, edit second**: when an imported component cannot be safely edited, preserve and preview it rather than rewriting it destructively.
6. **Small Codex tasks**: use repository docs as durable context and run Codex in PR-sized milestones.

## Recommended architecture

Default stack:

- Desktop shell: Tauri 2
- Frontend: React + TypeScript + Vite
- Visual editor foundation: GrapesJS for page-builder/editor state, with a simplified custom UI
- Precision movement: Moveable or interact.js for drag, resize, snap, and nudge interactions
- Import/export: parse5 for HTML parsing and JSZip for ZIP bundles
- Security: sandboxed preview, strict Content Security Policy, no remote network by default, no telemetry
- Testing: Vitest, Playwright, security fixtures, accessibility checks, import/export roundtrip fixtures
- Distribution: Windows user-scope installer or portable bundle generated by GitHub Actions

## Why this repo starts with Markdown only

This repository is intentionally documentation-first. Codex works better when the durable product goal, security model, architecture, validation rules, and task boundaries live inside the repo instead of inside a one-off prompt.

After creating the repo in GitHub, start Codex with the prompts in `codex/`.

## Current implementation state

Active development is currently a **browser-first static prototype**.

- Milestone 0 is complete.
- Milestone 1 is complete (static shell).
- Milestone 2A is complete (local `.html/.htm` scan-only intake).

Current app files:

- `apps/desktop/index.html`
- `apps/desktop/src/app-shell.mjs`
- `apps/desktop/src/importer.mjs`

Current capabilities:

- local file selection from app shell
- metadata-only project model
- local `.html/.htm` scan summary via `textContent`

Current non-capabilities:

- no ZIP parsing
- no preview rendering iframe/webview
- no export
- no visual editing
- no Tauri/React/Vite/TypeScript app code installed yet

## Repo map

```text
/
  README.md
  AGENTS.md
  MASTER_PLAN.md
  IMPLEMENTATION_RULES.md
  VALIDATION.md
  PROGRESS.md
  DECISIONS.md
  REPO_SETUP.md
  SECURITY.md
  CONTRIBUTING.md
  CHANGELOG.md
  LICENSE.md
  docs/
    ARCHITECTURE.md
    SECURITY_MODEL.md
    THREAT_MODEL.md
    IMPORT_EXPORT_SPEC.md
    EDITABLE_CONTRACT.md
    COPILOT_HTML_CONTRACT.md
    UX_PRINCIPLES.md
    TECH_STACK.md
    NON_ADMIN_DISTRIBUTION.md
    DATA_PRIVACY.md
    TEST_PLAN.md
    QUALITY_GATES.md
    RELEASE_CHECKLIST.md
    USER_GUIDE_DRAFT.md
    ROADMAP.md
    ISSUES_BACKLOG.md
  codex/
    CODEX_CLOUD_TASK_TEMPLATE.md
    CODEX_GOAL_TEMPLATE.md
    00_BOOTSTRAP_REPO.md
    01_FOUNDATION_SHELL.md
    02_IMPORT_EXPORT_PIPELINE.md
    03_EDITOR_MVP.md
    04_SECURE_PREVIEW.md
    05_PACKAGING_RELEASE.md
    06_AUDIT_AND_HARDENING.md
  .github/
    pull_request_template.md
    ISSUE_TEMPLATE/
      bug_report.md
      feature_request.md
      milestone_task.md
      security_review.md
```

## Non-goals for the MVP

- No cloud sync.
- No collaboration features.
- No user accounts.
- No telemetry.
- No AI calls inside the editor.
- No arbitrary website builder scope.
- No promise to safely edit every possible HTML/JS web app.

## MVP definition

The MVP is successful when a normal user can:

1. Open an HTML file or ZIP bundle generated by Microsoft 365 Copilot Chat.
2. Edit visible text.
3. Replace images and alt text.
4. Move and resize selected elements with drag handles and keyboard nudging.
5. Preview safely with scripts off by default.
6. Export a working HTML file or ZIP bundle.
7. Do all of the above offline and without administrator rights.
